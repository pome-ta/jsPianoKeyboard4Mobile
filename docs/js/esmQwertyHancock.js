'use strict';

let QwertyHancock;
const version = '0.0.0';

const settings = {
  id: 'keyboard',
  octaves: 3,
  width: 0,
  height: 0,
  margin: 0,
  startNote: 'A3',
  whiteKeyColour: '#fff',
  blackKeyColour: '#000',
  activeColour: 'yellow',
  borderColour: '#000',
  keyboardLayout: 'en',
  musicalTyping: false,
};

const init = function (us) {
  const user_settings = us || {};
};
